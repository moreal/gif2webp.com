/**
 * Shared style constants for consistent UI styling
 */

/**
 * Opacity value for footer interactive elements (buttons, links, selects)
 * in their default/resting state
 */
export const FOOTER_ELEMENT_OPACITY = 0.8;
